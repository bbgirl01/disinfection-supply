import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PackIndexPage } from './pack-index';

@NgModule({
  declarations: [
    // PackIndexPage,
  ],
  imports: [
    IonicPageModule.forChild(PackIndexPage),
  ],
})
export class PackIndexPageModule { }
